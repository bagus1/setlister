<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= gigDocument.title %> - <%= gigDocument.song.title %>
    </title>

    <!-- Quill Better Table CSS for displaying tables -->
    <link rel="stylesheet"
        href="https://cdn.jsdelivr.net/npm/quill-better-table@1.2.10/dist/quill-better-table.min.css">

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, Helvetica, sans-serif;
            font-size: 11pt;
            line-height: 1.2;
        }
        
        /* Remove the unused container rule */
        /* .container {
            margin-left: 0.75in;
        } */

        .song-title {
            font-size: 14pt;
            font-weight: bold;
            margin-bottom: 10px;
            text-align: center;
        }

        .document-content {
            margin: 0;
            padding: 0;
            margin-left: 0.75in;
        }

        .document-content p {
            margin: 0.2em 0;
            /* Allow inline line-height styles to take precedence */
        }

        /* Quill indentation classes */
        .ql-indent-1 {
            margin-left: 2em !important;
        }

        .ql-indent-2 {
            margin-left: 4em !important;
        }

        .ql-indent-3 {
            margin-left: 6em !important;
        }

        .ql-indent-4 {
            margin-left: 8em !important;
        }

        .ql-indent-5 {
            margin-left: 10em !important;
        }

        .ql-indent-6 {
            margin-left: 12em !important;
        }

        .ql-indent-7 {
            margin-left: 14em !important;
        }

        .ql-indent-8 {
            margin-left: 16em !important;
        }

        /* Font weight overrides for gig document content */
        .document-content {
            font-weight: 600 !important;
            /* Normal text = 600 */
        }

        .document-content strong,
        .document-content b {
            font-weight: 800 !important;
            /* Bold text = 800 */
        }

        /* Quill font family classes */
        .font-serif {
            font-family: "Times New Roman", Times, serif !important;
        }

        .font-sans {
            font-family: Arial, Helvetica, sans-serif !important;
        }

        .font-monospace {
            font-family: "Courier New", Courier, monospace !important;
        }

        /* Navigation buttons container */
        .nav-buttons {
            margin-bottom: 20px;
        }

        @media print {
            body {
                margin: 0 !important;
                padding: 0 !important;
                margin-left: 0.75in !important;
            }

            .song-title {
                margin-bottom: 5px !important;
            }

            .document-content {
                margin-left: 0 !important;
            }

            /* Hide navigation buttons when printing */
            .nav-buttons {
                display: none !important;
            }

            /* Hide the print button when printing */
            button {
                display: none !important;
            }
        }
    </style>
</head>

<body>

    <div class="document-content">
        <div style="font-size: 14pt; font-weight: bold; margin-bottom: 10px;">
        <%= gigDocument.song.title %>
        </div>
        <div class="nav-buttons">
            <button onclick="window.print()"
            style="padding: 8px 16px; font-size: 12pt; background: #007bff; color: white; border: none; border-radius: 4px; cursor: pointer; margin-right: 10px;">
            Print
        </button>
            <a href="/songs/<%= gigDocument.song.id %>/docs/<%= gigDocument.id %>"
            style="padding: 8px 16px; font-size: 12pt; background: #6c757d; color: white; text-decoration: none; border: none; border-radius: 4px; cursor: pointer; display: inline-block;">
            Back
        </a>
        </div>
        <% if (gigDocument.content && gigDocument.content.trim()) { %>
            <%- gigDocument.content %>
                <% } else { %>
                    <p>No content available.</p>
                    <% } %>
    </div>
</body>

</html>