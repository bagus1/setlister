<div class="row justify-content-center">
    <div class="col-md-6 col-lg-4">
        <div class="card">
            <div class="card-header">
                <h4 class="mb-0">
                    <i class="bi bi-person-plus"></i> Register
                </h4>
            </div>
            <div class="card-body">
                <% if (typeof intent !== 'undefined' && intent === 'record') { %>
                    <div class="alert alert-info">
                        <i class="bi bi-record-circle"></i>
                        <strong>Almost there!</strong> Create your account to start recording your rehearsals.
                    </div>
                <% } %>

                <% if (typeof errors !=='undefined' && errors.length> 0) { %>
                    <div class="alert alert-danger">
                        <ul class="mb-0">
                            <% errors.forEach(error=> { %>
                                <li>
                                    <%= error.msg %>
                                </li>
                                <% }) %>
                        </ul>
                    </div>
                    <% } %>

                        <form action="/auth/register" method="POST" id="registerForm">
                            <% if (typeof intent !== 'undefined' && intent) { %>
                                <input type="hidden" name="intent" value="<%= intent %>">
                            <% } %>
                            <div class="mb-3">
                                <label for="username" class="form-label">Username</label>
                                <input type="text" class="form-control" id="username" name="username"
                                    value="<%= typeof username !== 'undefined' ? username : '' %>" required>
                            </div>

                            <div class="mb-3">
                                <label for="email" class="form-label">Email</label>
                                <input type="email" class="form-control" id="email" name="email"
                                    value="<%= typeof email !== 'undefined' ? email : '' %>" required>
                            </div>

                            <div class="mb-3">
                                <label for="password" class="form-label">Password</label>
                                <input type="password" class="form-control" id="password" name="password" required>
                                <div class="form-text">Password must be at least 6 characters.</div>
                            </div>

                            <div class="mb-3">
                                <label for="confirmPassword" class="form-label">Confirm Password</label>
                                <input type="password" class="form-control" id="confirmPassword" name="confirmPassword"
                                    required>
                            </div>

                            <% if (typeof intent !== 'undefined' && intent === 'record') { %>
                                <div class="mb-3">
                                    <label for="bandName" class="form-label">Band Name</label>
                                    <input type="text" class="form-control" id="bandName" name="bandName"
                                        value="<%= typeof bandName !== 'undefined' ? bandName : '' %>" required>
                                    <div class="form-text">We'll create your first band to get you recording quickly</div>
                                </div>
                            <% } %>

                            <button type="submit" class="btn btn-primary w-100" id="registerBtn">
                                <% if (typeof intent !== 'undefined' && intent === 'record') { %>
                                    <i class="bi bi-record-circle"></i> Register & Start Recording
                                <% } else { %>
                                    Register
                                <% } %>
                            </button>
                        </form>

                        <script>
                            // Prevent double-click registration
                            document.getElementById('registerForm').addEventListener('submit', function(e) {
                                const submitBtn = document.getElementById('registerBtn');
                                if (submitBtn.disabled) {
                                    e.preventDefault();
                                    return false;
                                }
                                submitBtn.disabled = true;
                                submitBtn.innerHTML = '<i class="bi bi-hourglass-split"></i> Creating Account...';
                            });
                        </script>

                        <hr>

                        <p class="text-center mb-0">
                            Already have an account?
                            <a href="/auth/login?returnTo=<%= encodeURIComponent(currentUrl) %>">Login here</a>
                        </p>
            </div>
        </div>
    </div>
</div>