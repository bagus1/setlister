<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <style>
        body {
            font-family: 'Courier New', monospace;
            font-size: 12px;
            line-height: 1.4;
            margin: 0;
            padding: 20px;
            background: white;
        }

        .gig-header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #333;
            padding-bottom: 20px;
        }

        .gig-header h1 {
            margin: 0;
            font-size: 24px;
            font-weight: bold;
        }

        .gig-header h2 {
            margin: 5px 0;
            font-size: 18px;
            color: #666;
        }

        .gig-header .date {
            font-size: 16px;
            color: #888;
        }

        .song-section {
            page-break-inside: avoid;
            margin-bottom: 40px;
            border: 1px solid #ccc;
            padding: 15px;
        }

        .song-header {
            background: #f5f5f5;
            padding: 10px;
            margin: -15px -15px 15px -15px;
            border-bottom: 1px solid #ccc;
        }

        .song-title {
            font-size: 18px;
            font-weight: bold;
            margin: 0;
        }

        .song-meta {
            font-size: 12px;
            color: #666;
            margin: 5px 0 0 0;
        }

        .song-content {
            white-space: pre-wrap;
            font-family: 'Courier New', monospace;
            font-size: 11px;
            line-height: 1.3;
        }

        .set-divider {
            text-align: center;
            margin: 30px 0;
            font-size: 16px;
            font-weight: bold;
            color: #333;
            border-top: 1px solid #ccc;
            padding-top: 15px;
        }

        .page-break {
            page-break-before: always;
        }

        @media print {
            body {
                padding: 0;
            }

            .song-section {
                page-break-inside: avoid;
            }

            .set-divider {
                page-break-after: avoid;
            }
        }
    </style>
</head>

<body>
    <div class="gig-header">
        <h1>
            <%= setlist.title %>
        </h1>
        <h2>
            <%= setlist.Band.name %>
        </h2>
        <% if (setlist.date) { %>
            <div class="date">
                <%= new Date(setlist.date).toLocaleDateString('en-US', { weekday: 'long' , year: 'numeric' ,
                    month: 'long' , day: 'numeric' }) %>
            </div>
            <% } %>
    </div>

    <% if (setlist.SetlistSets && setlist.SetlistSets.length> 0) { %>
        <% setlist.SetlistSets.sort((a, b)=> a.order - b.order).forEach((set, setIndex) => { %>
            <% if (set.SetlistSongs && set.SetlistSongs.length> 0) { %>
                <% if (setIndex> 0) { %>
                    <div class="set-divider">
                        --- <%= set.name %> ---
                    </div>
                    <% } %>

                        <% set.SetlistSongs.sort((a, b)=> a.order - b.order).forEach((setlistSong, songIndex) => { %>
                            <% const preferredDocId=preferredGigDocuments[setlistSong.Song.id]; const
                                gigDocument=preferredDocId ? gigDocumentMap[preferredDocId] : null; %>

                                <div class="song-section">
                                    <div class="song-header">
                                        <div class="song-title">
                                            <%= songIndex + 1 %>. <%= setlistSong.Song.title %>
                                        </div>
                                        <div class="song-meta">
                                            <% if (setlistSong.Song.Artists && setlistSong.Song.Artists.length> 0) { %>
                                                by <%= setlistSong.Song.Artists[0].name %>
                                                    <% } %>
                                                        <% if (setlistSong.Song.key) { %>
                                                            • Key: <%= setlistSong.Song.key %>
                                                                <% } %>
                                                                    <% if (setlistSong.Song.time) { %>
                                                                        • Time: <%= Math.floor(setlistSong.Song.time /
                                                                            60) %>:<%= (setlistSong.Song.time %
                                                                                60).toString().padStart(2, '0' ) %>
                                                                                <% } %>
                                                                                    <% if (gigDocument) { %>
                                                                                        • <%=
                                                                                            gigDocument.getTypeDisplayName()
                                                                                            %> v<%= gigDocument.version
                                                                                                %>
                                                                                                <% } %>
                                        </div>
                                    </div>

                                    <% if (gigDocument && gigDocument.content && gigDocument.content.trim()) { %>
                                        <div class="song-content">
                                            <%- gigDocument.content %>
                                        </div>
                                        <% } else { %>
                                            <div class="song-content" style="color: #999; font-style: italic;">
                                                No gig document content available for this song.
                                                <% if (preferredDocId) { %>
                                                    The selected document is empty. (Doc ID: <%= preferredDocId %>)
                                                        <% } else { %>
                                                            No preferred gig document has been selected.
                                                            <% } %>
                                            </div>
                                            <% } %>

                                                <!-- Debug info -->
                                                <div
                                                    style="font-size: 10px; color: #ccc; margin-top: 10px; border-top: 1px solid #eee; padding-top: 5px;">
                                                    Debug: Song ID: <%= setlistSong.Song.id %>,
                                                        Preferred Doc ID: <%= preferredDocId || 'none' %>,
                                                            Has Gig Doc: <%= !!gigDocument %>,
                                                                Content Length: <%= gigDocument?.content?.length || 0 %>
                                                </div>
                                </div>

                                <% if (songIndex < set.SetlistSongs.length - 1) { %>
                                    <div style="height: 20px;"></div>
                                    <% } %>
                                        <% }); %>
                                            <% } %>
                                                <% }); %>
                                                    <% } else { %>
                                                        <div style="text-align: center; padding: 50px; color: #999;">
                                                            <h3>No songs in this setlist</h3>
                                                            <p>Add songs to the setlist before generating the gig view.
                                                            </p>
                                                        </div>
                                                        <% } %>

                                                            <div
                                                                style="margin-top: 50px; text-align: center; color: #999; font-size: 10px;">
                                                                Generated on <%= new Date().toLocaleString() %>
                                                            </div>
</body>

</html>